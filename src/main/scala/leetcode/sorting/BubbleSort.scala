package leetcode.sorting

object BubbleSort extends App {


  private def execute(list: List[String]) = {
    val n = list.length
    for (i <- list.indices) {
      var swap = false
      for (j <- i to n - i - 1) {

      }

    }


  }


}
